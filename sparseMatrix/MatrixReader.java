import java.util.Scanner;
import java.io.File;
import java.io.IOException;

public class MatrixReader {

    public SparseMatrix read(String file) throws IOException{
        File matrixFile = new File("matrixA.txt");
        Scanner scanner = new Scanner(matrixFile);

        int rows =

        SparseMatrix matrix = new SparseMatrix(0, 0);

        return null;
    }
}
